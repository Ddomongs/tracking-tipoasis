import { InternalCsHelper } from "@/components/InternalCsHelper";

export const metadata = {
  title: "내부 CS 안내 도구",
  description: "배송 안내 및 통관부호 불일치 문자 준비 도구"
};

export default function InternalCsHelperPage() {
  return <InternalCsHelper />;
}
